<form method="post" style="display:inline;" action="{{ path('app_compte_divisionnaire_delete', {'id': compte_divisionnaire.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer cet enregistrement ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ compte_divisionnaire.id) }}">
    <button class="btn-danger" title="supprimer">
        <span class="fa fa-trash-o"></span>
    </button>
</form>
<form method="post" style="display:inline;" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer cet enregistrement ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn-danger" title="supprimer">
        <span class="fa fa-trash-o"></span>
    </button>
</form>